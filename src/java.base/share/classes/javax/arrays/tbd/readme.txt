These files are not specific to arrays or matrices, but are either necessary
to the implementation (e.g., signature of a matrix fill function) or useful
for both clients and testing (Complex and DComplex -- eventually these should
be value types).
